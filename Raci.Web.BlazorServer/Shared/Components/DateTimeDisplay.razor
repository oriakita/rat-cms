
@if (Value != null)
{
    @Value.Value.AddHours(Gtm).ToString(Format)
}
else
{
    @("-")
}


@code {
    [Parameter]
    public DateTime? Value { get; set; }

    [Parameter]
    public string Format { get; set; } = "dd/MM/yyyy HH:mm:ss";

    [Parameter]
    public int Gtm { get; set; } = 7;
}
